# Kubernetes Service for CAP Verifier API
apiVersion: v1
kind: Service
metadata:
  name: cap-verifier-api
  labels:
    app: cap-verifier-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: cap-verifier-api
