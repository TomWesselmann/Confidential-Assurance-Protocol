# PersistentVolumeClaim for CAP Agent Build Directory
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cap-build-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
